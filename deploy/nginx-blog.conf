# 个人博客 Nginx 反向代理
# 由 setup-server.sh 安装，并把 __DOMAIN__ 替换为你的域名
# 之后 certbot --nginx 会自动注入 HTTPS（443）配置并重写 80 -> 443 跳转

server {
    listen 80;
    listen [::]:80;
    server_name __DOMAIN__;

    client_max_body_size 8m;

    # 安全响应头
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Permissions-Policy "geolocation=(), microphone=(), camera=()" always;

    # 静态资源：nginx 直接托管，提升性能
    location /static/ {
        alias /opt/blog/static/;
        expires 7d;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    # 应用请求：反代到 gunicorn（仅监听 127.0.0.1:5000，不暴露公网）
    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_redirect off;
        proxy_connect_timeout 30s;
        proxy_read_timeout 60s;
    }
}

# ============================================================
# 手动 HTTPS 模板（可选）：不用 certbot --nginx 时取消注释并填好证书路径
# ============================================================
# server {
#     listen 443 ssl;
#     listen [::]:443 ssl;
#     server_name __DOMAIN__;
#     ssl_certificate     /etc/letsencrypt/live/__DOMAIN__/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/__DOMAIN__/privatekey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     client_max_body_size 8m;
#     location /static/ { alias /opt/blog/static/; expires 7d; try_files $uri =404; }
#     location / {
#         proxy_pass http://127.0.0.1:5000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_redirect off;
#     }
# }
